/**
 * Internal dependencies
 */
import BlockStack from '@/components/block-stack/block-stack';

const FormControl = (props) => {
    return <BlockStack gap="150" {...props} />;
};

export default FormControl;
